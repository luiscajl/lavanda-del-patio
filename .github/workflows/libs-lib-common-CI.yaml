name: Libs:LibCommon:Release
on:
  push:
    branches:
      - main
      - 'feature/**'
    paths:
      - "libs/lib-common/**"
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0