apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qbittorrent-client.fullname" . }}-test-connection"
  labels:
    {{- include "qbittorrent-client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qbittorrent-client.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
