package es.lavanda.lib.common.service;

import org.springframework.beans.factory.annotation.Autowired;
// import org.springframework.cloud.aws.messaging.core.NotificationMessagingTemplate;
import org.springframework.stereotype.Service;


// @Service
public class NotificationService {
    
    // @Autowired
    // private NotificationMessagingTemplate messagingTemplate;

    // public void send(SnsTopic topic, String message, String subject) {
        // messagingTemplate.sendNotification(topic.getValue(), message, subject);
    // }

}
